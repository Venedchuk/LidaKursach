@model LidaKursach.Models.Book
@using LidaKursach.Models
@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Carts</h4>
        <hr />
        @*@Html.ValidationSummary(true, "", new { @class = "text-danger" })
            <div class="form-group">
                @Html.LabelFor(model => model, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(Model., new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.Start_reading, DateTime.Now.ToString(), new { @class = "text-danger" })
                </div>
            </div>*@

        @*@foreach (var book in (List<Book>)ViewBag.Book)
        {
            <div>
                @book.Title
            </div>
        }*@

    @Html.DropDownListFor(model => model.Id,new SelectList(ViewBag.AllBook,"Id","Title"))

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Get Book" class="btn btn-default" />
        </div>
    </div>
</div>

}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
